[project]
name = "mediapipe-hand-ascend310b"
version = "0.1.0"
description = "MediaPipe hand model extraction, evaluation, conversion, and Ascend 310B deployment helpers."
requires-python = ">=3.9"
dependencies = [
  "numpy",
  "opencv-python",
  "ai-edge-litert",
]
